mkdocs>=1.6,<2.0
mkdocs-material>=9.5,<10.0
mkdocs-llmstxt-md>=0.2
mkdocs-awesome-nav>=3.3,<4.0

# Imported directly by scripts/validate_recipes.py and
# scripts/check_staleness.py. It currently arrives as a transitive mkdocs
# dependency, which means a mkdocs release dropping it would break the scripts
# with nothing in this file explaining why.
PyYAML>=6.0

# Test runner for tests/, which pins the branch-resolution behaviour of
# scripts/check_staleness.py against committed drupal.org fixtures. Nothing in
# this repo imports pytest at runtime; it is listed here because
# .github/workflows/pr-check.yml installs only this file and then needs a
# runner to invoke.
pytest>=8.0

# Preserves published guide URLs when a partition slug changes. A renamed guide
# is invisible to llms.txt (which links only topic indexes) but 404s for anyone
# holding a direct link.
mkdocs-redirects>=1.2
